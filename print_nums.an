fibbonacci = fun(num)
    if num < 2
        return 1
    
    return fibbonacci(num - 1) + fibbonacci(num - 2)

main = fun() 
    i = 0
    while i < 30
        println(fibbonacci(i))
        i += 1


main()